---
sidebar_position: 3
slug: /k3/reference-solutions/humanoid/foundation/runtime-communication-safety
---

# 运行时通信与安全

本页说明人形 SDK 各运行进程之间的数据流、通信方式和状态安全边界。

## 1. FSM 完整链路

FSM 模式同时运行三个进程：

| 进程 | 职责 | 主要数据 |
| --- | --- | --- |
| `hmi_runtime` | 发送状态切换、策略选择和速度请求；显示 Control 端回传的真实状态 | `Command` →，`ControlStatus` ← |
| `control_runtime` | 接收机器人状态与 HMI 请求，运行行为状态机和策略，输出关节控制指令 | `RobotData` ←，`Command` ←，`ControlCmd` → |
| `driver_runtime` | 根据配置创建仿真或实机后端，在后端数据与公共数据契约之间转换 | `RobotData` →，`ControlCmd` ← |

`control_runtime` 中的行为状态机包含 `POWER_OFF → DAMP → HOME → ZERO → RL` 的正常进入链路和 `SAFETY` 安全状态。`StateRL` 在独立线程中执行策略推理，避免将 ONNX 推理直接串联到主控制循环。

`behavior_manager/policy_adapter` 是行为管理器的内部扩展点，用于将 MJLab、ProtoMotions 和 SONIC 等参考动作或特殊模型输入转换为通用 `PolicyExecutor` 可消费的数据。对于启用 SONIC Kinematic Planner 的配置，Planner 在独立线程中根据速度命令生成参考轨迹；低层关节跟踪仍由 SONIC tracker 策略执行。

## 2. Sim2Sim 简化链路

`control_sim2sim_runtime` 直接调用 `PolicyExecutor` 并与 `driver_runtime` 交换仿真状态和关节目标，不经过 HMI、FSM 和 `policy_adapter`。它适合验证已能由通用策略接口完整描述的 ONNX 策略，不代表完整运行时或实机安全链路已验证。

## 3. 驱动后端选择

`driver_runtime` 读取 YAML 中的 `driver.backend`，在当前构建已包含的后端中选择：

- `mujoco`：将 MuJoCo 的 `SimState` / `SimControl` 与 SDK 公共数据转换。仿真组件可在 x86_64 和 riscv64/K3 构建；跨机流程通常由 PC 运行 MuJoCo、K3 运行控制端。
- `whole_body`：将 SDK 公共指令转为整机硬件指令，并回传电机、关节和 IMU 状态。它支持机型配置的直接关节与并联脚踝映射、只读启动、命令看门狗和故障锁存等能力。

选择了未编入当前产物的后端时，`driver_runtime` 会在启动阶段明确失败，不会静默切换到其他后端。

## 4. 进程通信

运行时之间通过 `transport` 交换同一组语义数据，通信实现由 `transport.type` 选择：

- `shm`：同机多进程共享内存通信。
- `udp`：同机或 PC + K3 跨机通信。

SHM 与 UDP 共用协议版本和状态、命令语义；协议不匹配时拒绝解析，不使用旧布局继续运行。

日志是 `humanoid_common` 内的跨运行时能力，可按机型配置记录进程事件、控制状态和驱动遥测。遥测通过有界后台队列落盘，不改变公共数据契约。

## 5. 安全边界

- 仿真后端用于验证状态机、通信和控制链，不替代实机的驱动、安全和急停验收。
- `whole_body` 的看门狗、故障锁存等能力属于底层保护；实机上线仍需结合机器人本体完成方向、零位、限位和急停检查。
- Sim2Sim 跳过 HMI 与 FSM，只能证明策略在简化链路中运行，不能作为完整实机链路的验收结果。

整体软件分层见[SDK 架构](3.4.2.2-SDK架构.md)，硬件侧职责见[整机控制与硬件后端](../3.4.3-本体接入/3.4.3.3-整机控制与硬件后端.md)。
