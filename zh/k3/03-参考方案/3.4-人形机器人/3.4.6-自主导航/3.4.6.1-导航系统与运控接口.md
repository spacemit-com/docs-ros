---
sidebar_position: 1
slug: /k3/reference-solutions/humanoid/navigation/control-bridge
draft: true
---

# 导航系统与运控接口

本节定义导航系统与人形全身运控之间的适配边界。导航模块负责给出目标和路径约束，运控模块负责判断当前状态是否允许执行，并输出机器人能够安全跟踪的运动。

## 输入与输出契约

接口需要明确目标位姿、速度指令、时间戳、参考坐标系和有效期，以及机器人位姿、运动模式、控制状态和故障状态等反馈。导航指令不能绕过运控状态机直接驱动关节执行器。

<!-- TODO: 以 middleware/ros2/control/humanoid 的 /cmd_vel → Native HMI 桥接为现有入口，补充消息、字段、单位、频率和超时规则；未实现的任务接口另标为规划。 -->

## 命令转换与模式切换

导航输出需要经过限幅、坐标变换和运控能力约束，再映射为当前策略或控制器接受的命令。站立、行走、转向、停止和人工接管之间的切换条件应由统一的行为管理机制处理。

<!-- TODO: 补充 Nav2、humanoid_nav_rl_bridge_node 与 Native FSM 的映射表、模式切换时序和人工接管边界。 -->

## 执行反馈

运控侧应向导航侧返回命令接收、执行中、完成、拒绝和异常等状态，使上层能够判断继续规划、重新规划还是终止任务。

<!-- TODO: 补充反馈状态、失败原因和上层处理建议。 -->

## 安全中断

人工停止、失联、定位失效、指令超时或运控异常应具备高于普通导航命令的中断优先级。中断后需要进入明确的安全状态，并向导航与任务层同步结果。

<!-- TODO: 补充各类中断的触发条件、安全动作和恢复流程。 -->
